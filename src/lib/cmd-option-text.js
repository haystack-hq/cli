module.exports = {
    identifier: 'name of stack. If omitted, the stack from the current project folder will be used',
    excludeMount: 'no file system mounts to the stack will be created',
    directory: 'relative or full path to directory to mount to on the local machine'
}